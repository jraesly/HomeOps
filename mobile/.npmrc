# openapi-typescript@7 declares a TypeScript ^5 peer, but this project uses
# TS 6 (they're compatible in practice — it's a codegen dev tool). Relax peer
# resolution so npm ci works everywhere: locally, on EAS build, and in CI.
legacy-peer-deps=true
